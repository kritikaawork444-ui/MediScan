# Leave empty for local dev (Vite proxies /api → localhost:8000)
# For production (Vercel/Netlify), set your public API URL (no trailing slash):
# VITE_API_URL=https://mediscan-api.onrender.com

VITE_API_URL=
